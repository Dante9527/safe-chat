# SafeChat — Application dependencies
# =====================================
# Heavy deps (sentence-transformers, chromadb, torch) live in
# requirements-heavy.txt and are installed first as a separate Docker layer.

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18
slowapi==0.1.9

# RAG / LLM
# LangChain 套件互相依賴，langchain-core 版本由 pip 自動解析以避免衝突
# (langchain-ollama>=0.3.2 需要 langchain-core>=0.3.52，嚴格釘版 0.3.30 會衝突)
langchain>=0.3.14,<0.4.0
langchain-community>=0.3.14,<0.4.0

# Document loaders
pypdf==5.1.0

# HTTP
requests==2.32.3
httpx>=0.27.0

# Ollama support — langchain_ollama replaces deprecated langchain_community.chat_models.ollama
langchain-ollama==0.3.2
